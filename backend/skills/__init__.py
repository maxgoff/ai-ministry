"""Stage-0 grounding skills package.

Each skill implements GroundingSkill (see base.py) and contributes a briefing
fragment that backend/grounding.py merges into the single Stage-0 briefing the
council injects into Stages 1-3.
"""
